# pyproject.toml is the single source of truth for dependencies and their
# security floors — this file exists only so `pip install -r requirements.txt`
# keeps working, and must not list packages or pins of its own.
-e .[dev]
